<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper 
   PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="sample">
	<insert id="insertBoard" parameterType="com.ktds.board.vo.BoardVO">
		INSERT INTO public.board(
			no,
			title,
			body,
			viewcnt,
			writedate
        ) 
    	VALUES (
    		nextval('no'),
    		#{title},
    		#{body},
<!--     		#{param2}, -->
<!--     		'dd', -->
    		0,
    		to_char(now(), 'YYYY-MM-DD hh:MI')
		);
		
	</insert>
	
	<select id="readBoardList" resultType="hashmap">
		select * from public.board order by writedate desc;
	</select>
	
</mapper>